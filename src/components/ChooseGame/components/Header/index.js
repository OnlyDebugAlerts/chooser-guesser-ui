import React from 'react';

import './index.scss';

export default () => {
    const points = [];
    return (
    <div className="header">
        <div className="box_points">{points}</div>
    </div>
    )
}